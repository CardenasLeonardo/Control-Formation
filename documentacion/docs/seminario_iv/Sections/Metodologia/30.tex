\section{Integración de la no holonomía}
\setlength{\columnseprule}{0pt}
\twocolumn
\subsection{Error de coordenadas polares}
Error de la meta con respecto al estado actual \cite{ramirez2001control}:
\begin{equation*}
    \mathbf{z}(t) = \begin{bmatrix} a \\ \alpha \end{bmatrix}
\end{equation*}
\subsection{Ley de control}
\vspace{-0.5cm}
\begin{equation*}
    \mathbf{u} =
    \begin{bmatrix} v \\ \omega \end{bmatrix} =
    \begin{bmatrix}
        k_1\, a \cos\alpha \\
        k_2\,\alpha + k_1 \sin\alpha \cos\alpha
    \end{bmatrix}
\end{equation*}

\begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{/home/calafaker/Control-Formation/documentacion/figuras/personales/trajectory_individual (1).pdf}
    \caption{Navegación autónoma de un robot diferencial}
    \label{fig:trayectoria_individual}
\end{figure}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Ley de Control}
\subsection{Diseño de Trayectorias sin Reversas Innecesarias}

La curva característica del robot guía el desarrollo de la estructura virtual y su generador de trayectorias.
\begin{center}
\begin{tikzpicture}[
  font=\fontfamily{ptm}\selectfont\scriptsize,
  >={Stealth[length=2mm]},
  block/.style={draw, minimum width=1.3cm, minimum height=1.3cm, align=center, inner sep=2pt},
]
  \node[block] (VS) {Estructura\\Virtual};
  \node[block, right=1.1cm of VS] (C)  {Consenso};
  \node[block, right=1.1cm of C]  (CL) {Ley de\\Control};
  \node[block, right=1.1cm of CL] (F)  {PVA};

  \node[align=center, above=0.7cm of C] (NB) {Vecindad};
  \node[align=center, above=0.7cm of F] (LI) {LiDAR};

  \draw[->] (VS) -- node[above]{$x_v$} node[below]{$[x,y]$} (C);
  \draw[->] (C)  -- node[above]{$z$} node[below]{$[a,\alpha]$} (CL);
  \draw[->] (CL) -- node[above]{$u_{ref}$} node[below]{$[v,\omega]$} (F);
  \draw[->] (NB) -- (C);
  \draw[->] (LI) -- (F);

  \coordinate (uEnd) at ($(F.east)+(0.8,0)$);
  \draw[->] (F.east) -- node[above]{$u^*$} node[below]{$[v,\omega]$} (uEnd);

  \draw[dashed, green!60!black] ($(VS.north west)+(-0.2,0.2)$) rectangle ($(VS.south east)+(0.2,-0.2)$);
  \draw[dashed, green!60!black] ($(CL.north west)+(-0.2,0.2)$) rectangle ($(CL.south east)+(0.2,-0.2)$);

  \draw[dashed, green!60!black, ->] ($(CL.south west)+(-0.2,-0.2)$) to[out=220, in=-40]
    node[below, pos=0.5] {\itshape naturaleza del movimiento}
    ($(VS.south east)+(0.2,-0.2)$);
\end{tikzpicture}
\end{center}

\begin{center}
\footnotesize\textbf{Figura~\ref{fig:flujo_control}:} Retroalimentación conceptual entre la ley de control y la generación de referencias.
\end{center}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Evasión de Colisiones}
\subsection{Limitación de la Velocidad de Referencia}

El bloque PVA proyecta la velocidad de referencia sobre el espacio de velocidades factibles, respetando las lecturas de los obstáculos detectados.

\begin{center}
\begin{tikzpicture}[
  font=\fontfamily{ptm}\selectfont\scriptsize,
  >={Stealth[length=2mm]},
  block/.style={draw, minimum width=1.3cm, minimum height=1.3cm, align=center, inner sep=2pt},
]
  \node[block] (VS) {Estructura\\Virtual};
  \node[block, right=1.1cm of VS] (C)  {Consenso};
  \node[block, right=1.1cm of C]  (CL) {Ley de\\Control};
  \node[block, right=1.1cm of CL] (F)  {PVA};

  \node[align=center, above=0.7cm of C] (NB) {Vecindad};
  \node[align=center, above=0.7cm of F] (LI) {LiDAR};

  \draw[->] (VS) -- node[above]{$x_v$} node[below]{$[x,y]$} (C);
  \draw[->] (C)  -- node[above]{$z$} node[below]{$[a,\alpha]$} (CL);
  \draw[->] (CL) -- node[above]{$u_{ref}$} node[below]{$[v,\omega]$} (F);
  \draw[->] (NB) -- (C);
  \draw[->] (LI) -- (F);

  \coordinate (uEnd) at ($(F.east)+(0.8,0)$);
  \draw[->] (F.east) -- node[above]{$u^*$} node[below]{$[v,\omega]$} (uEnd);

  \draw[dashed, blue] ($(F.north west)+(-0.2,0.2)$) rectangle ($(F.south east)+(0.2,-0.2)$);
\end{tikzpicture}
\end{center}

\begin{center}
\footnotesize\textbf{Figura~\ref{fig:flujo_control}:} El PVA limita la velocidad de referencia en función de los obstáculos detectados.
\end{center}